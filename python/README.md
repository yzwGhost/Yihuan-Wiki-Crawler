# Python Skeleton

当前阶段只提供 Python Playwright 目录骨架，不实现真实爬虫。

建议初始化命令：

```bash
cd python
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt
playwright install
python main.py
```
