# Tasks

- [x] Add OpenSpec change files for sequential all-character crawling
- [x] Extend shared crawler types and store state for mode and richer progress events
- [x] Enable `mode=all` in the Electron crawler IPC and Python service parsing
- [x] Implement Python discovery, candidate link persistence, and sequential batch crawling
- [x] Update the crawl task page for all-mode behavior and progress presentation
- [x] Verify build and Python all-mode behavior
- [x] Tighten discovery and page validation so non-character pages are skipped instead of being saved as character data
